<Page x:Class="VoiceInk.Windows.UI.Views.SettingsPages.PowerModePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      d:DesignHeight="450" d:DesignWidth="550"
      Title="PowerModePage">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="Power Mode" FontSize="20" FontWeight="Bold" Margin="0 0 0 5"/>
            <TextBlock Text="Automatically set a custom prompt based on the active application or browser URL." TextWrapping="Wrap"/>
        </StackPanel>

        <DataGrid x:Name="RulesGrid" Grid.Row="1" ItemsSource="{Binding PowerModeRules}" AutoGenerateColumns="False" CanUserAddRows="False" Margin="0 10" Height="200">
            <DataGrid.Columns>
                <DataGridTextColumn Header="If" Binding="{Binding ConditionType}" Width="Auto"/>
                <DataGridTextColumn Header="Operator" Binding="{Binding ConditionOperator}" Width="Auto"/>
                <DataGridTextColumn Header="Value" Binding="{Binding ConditionValue}" Width="*"/>
                <DataGridTextColumn Header="Set Prompt To" Binding="{Binding Prompt}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
        <Button Content="Delete Selected Rule" Click="DeleteButton_Click" Grid.Row="2" HorizontalAlignment="Right"/>

        <Grid Grid.Row="3" Margin="0 20 0 0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBlock Text="Add New Rule" FontWeight="Bold" Grid.Row="0"/>
            <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0 5 0 0">
                <ComboBox x:Name="ConditionTypeComboBox" ItemsSource="{Binding ConditionTypes}" SelectedItem="{Binding NewRule.ConditionType}" Width="100" Margin="0 0 5 0"/>
                <ComboBox x:Name="OperatorComboBox" ItemsSource="{Binding ConditionOperators}" SelectedItem="{Binding NewRule.ConditionOperator}" Width="100" Margin="0 0 5 0"/>
                <TextBox x:Name="ValueTextBox" Text="{Binding NewRule.ConditionValue, UpdateSourceTrigger=PropertyChanged}" Width="120" Margin="0 0 5 0"/>
                <TextBox x:Name="PromptTextBox" Text="{Binding NewRule.Prompt, UpdateSourceTrigger=PropertyChanged}" Width="120" Margin="0 0 5 0"/>
                <Button Content="Add" Click="AddButton_Click" Width="50"/>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
