<Page x:Class="VoiceInk.Windows.UI.Views.SettingsPages.DictionaryPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      d:DesignHeight="350" d:DesignWidth="450"
      Title="DictionaryPage">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Custom Dictionary" FontSize="20" FontWeight="Bold" Margin="0 0 0 10" Grid.Row="0"/>

        <DataGrid x:Name="ReplacementsGrid" Grid.Row="1" ItemsSource="{Binding WordReplacements}" AutoGenerateColumns="False" CanUserAddRows="False" Height="150">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Find" Binding="{Binding Find}" Width="*"/>
                <DataGridTextColumn Header="Replace With" Binding="{Binding ReplaceWith}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
        <Button x:Name="DeleteButton" Content="Delete Selected" Click="DeleteButton_Click" Grid.Row="2" HorizontalAlignment="Right" Margin="0 5"/>

        <Grid Grid.Row="3" Margin="0 20 0 0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Margin="0 0 5 0">
                <TextBlock Text="Find:"/>
                <TextBox x:Name="FindTextBox"/>
            </StackPanel>
            <StackPanel Grid.Column="1" Margin="5 0 0 0">
                <TextBlock Text="Replace with:"/>
                <TextBox x:Name="ReplaceWithTextBox"/>
            </StackPanel>
            <Button x:Name="AddButton" Content="Add" Click="AddButton_Click" Grid.Column="2" VerticalAlignment="Bottom" Width="75" Height="24"/>
        </Grid>
    </Grid>
</Page>
